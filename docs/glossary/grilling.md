## Grilling

<!-- d10e: auto-prune -->
<!-- Copier-vendored from the agentic-engineering-template — do NOT edit
     here; change it in the template and pull via `copier update`. The
     auto-prune marker above lets `disambiguate prune` remove this term
     from a repo that never links it. -->

The structured interview loop that stress-tests a plan via multiple-choice
questions in a [session](agent-session.md): options carry if-clauses, exactly one
carries a scored prediction, and each ruling lands as a
[decision record](decision-record.md) with rejection reasons captured as a
side effect of choosing.
