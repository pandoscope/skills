## User-Invoked

A skill whose `description` is stripped, reachable only by a human typing its name.
No [context load](context-load.md), paid for in [cognitive load](cognitive-load.md).
Nothing but the human can reach it: with no description, no other skill can fire it.

_Avoid_: procedure, workflow, command
