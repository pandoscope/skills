## No-Op

*Failure mode.*
A line the model already obeys by default, so you pay load to say nothing.
The test: does it change behavior versus the default?
Model-relative, not reader-relative — two people disagreeing about whether a line is a no-op
disagree about the default, and settle it by running the skill.

_Avoid_: redundant instruction, restating the obvious
