## Model-Invoked

A skill that keeps its [description](description.md),
so the agent finds and fires it on its own, and other skills can reach it.
The human can still type its name — model-invocation adds agent reach, never removes human reach.
Pays permanent [context load](context-load.md).

_Avoid_: ability, tool, capability
