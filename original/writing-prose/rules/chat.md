# Chat rules

These rules hold for replies a human reads in the conversation.

- `chat-register` [M] Keep chat natural and terse, because a human reads it.
- `chat-sentences` [M] Write full sentences, with fragments only for status and never for reasoning.
