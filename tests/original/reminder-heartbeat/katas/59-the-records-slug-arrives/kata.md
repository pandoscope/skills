# The record's slug arrives

**Rule (skills#46, check 8):** the green is the observed file — a
decisions/ name carrying the declared slug, committed inside the
turn's boundary. The declaration only scoped the question.

## What the fixture freezes

`rulings: seal-outside-machine` declared, and the store's checkout
gained `...seal-outside-machine.json` at 15:16 — one minute into the
turn.

## Expected

The turn seals and exits 0.
