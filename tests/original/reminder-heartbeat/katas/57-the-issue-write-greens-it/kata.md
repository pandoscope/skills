# The issue write greens it

**Rule (skills#46, check 4):** the check passes on the observed
call, not the declaration. Here the transcript carries an
`add_issue_comment` naming the declared ticket, so the diff of
declared against written is empty and the turn seals.

## What the fixture freezes

A green turn: `tickets: o/r#61` declared and an issue-write tool call
for exactly that ticket inside the turn's boundary.

## Expected

The turn seals and exits 0.
